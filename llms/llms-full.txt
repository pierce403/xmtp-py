---
description: >
  Full LLM reference for the xmtp-py monorepo.
---

# xmtp-py LLM Reference (Full)

> NOTE
> This file is generated automatically from the xmtp-py repository.
> Generated at: 3:12 AM UTC / January 22, 2026
> Included Sections:
> - README.md
> - sdks/python-sdk/README.md
> - sdks/agent-sdk/README.md
> - docs/index.rst
> - docs/python_sdk.rst
> - docs/agent_sdk.rst
> - docs/examples.rst
> - docs/migration_guide.rst
> - docs/contributing.rst
> - bindings/python/README.md
> - content-types/content-type-group-updated/README.md
> - content-types/content-type-markdown/README.md
> - content-types/content-type-primitives/README.md
> - content-types/content-type-reaction/README.md
> - content-types/content-type-read-receipt/README.md
> - content-types/content-type-remote-attachment/README.md
> - content-types/content-type-reply/README.md
> - content-types/content-type-text/README.md
> - content-types/content-type-transaction-reference/README.md
> - content-types/content-type-wallet-send-calls/README.md
> - api:Python SDK
> - api:Agent SDK
> - api:Content Types
> - api:Bindings

## Repository Documentation

### README.md

# xmtp

Unofficial Python client SDKs for the XMTP network.

Primary goal: feature parity with xmtp-js.

This is a community project that mirrors the structure and interfaces of the official [xmtp-js](https://github.com/xmtp/xmtp-js) SDK, adapted for Python.

## What's inside?

### SDKs

- [`python-sdk`](sdks/python-sdk): XMTP client SDK for Python
- [`agent-sdk`](sdks/agent-sdk): XMTP agent SDK for Python (event-driven, middleware-powered)

### Content types

- [`content-type-primitives`](content-types/content-type-primitives): Primitives for building custom XMTP content types
- [`content-type-group-updated`](content-types/content-type-group-updated): Content type for group update messages
- [`content-type-reaction`](content-types/content-type-reaction): Content type for reactions to messages
- [`content-type-read-receipt`](content-types/content-type-read-receipt): Content type for read receipts
- [`content-type-remote-attachment`](content-types/content-type-remote-attachment): Content type for file attachments stored off-network
- [`content-type-reply`](content-types/content-type-reply): Content type for direct replies to messages
- [`content-type-text`](content-types/content-type-text): Content type for plain text messages
- [`content-type-transaction-reference`](content-types/content-type-transaction-reference): Content type for on-chain transaction references
- [`content-type-markdown`](content-types/content-type-markdown): Content type for markdown-formatted messages
- [`content-type-wallet-send-calls`](content-types/content-type-wallet-send-calls): Content type for wallet transaction requests

## Requirements

- Python 3.10+
- libxmtp bindings >= 1.7.0-r3

## Installation

```bash
pip install xmtp
```

This installs the client SDK, agent SDK, and built-in content types in one package.

### Install from GitHub main

```bash
pip install "xmtp @ git+https://github.com/pierce403/xmtp-py.git@main"
```

### From source (dev)

Clone the repo and install into a virtualenv. Install the bindings first so
the local editable package can resolve them:

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -U pip

pip install -e bindings/python
pip install -e ".[dev]"
```

## Quick start

### Agent SDK (recommended)

```python
import asyncio
import xmtp

from xmtp_agent import Agent
from xmtp_agent.name_resolver import create_name_resolver
from xmtp_agent.user import create_user, create_signer
from xmtp.types import ClientOptions, LogLevel

recipient = "deanpierce.eth"


async def main() -> None:
    # Create a user and signer
    user = create_user()
    signer = create_signer(user)

    # Create the agent
    agent = await Agent.create(
        signer,
        ClientOptions(
            env="dev",
            disable_history_sync=True,
            logging_level=LogLevel.WARN,
            db_path=None,
        ),
    )

    # Resolve the recipient and send a message
    resolver = create_name_resolver()
    address = await resolver(recipient)
    if address is None:
        raise ValueError(f'Could not resolve address for "{recipient}"')

    dm = await agent.client.conversations.new_dm(address)
    await dm.send(f"Hello from xmtp-py {xmtp.__version__}")


if __name__ == "__main__":
    asyncio.run(main())
```

### Python SDK (client)

```python
import asyncio
import xmtp

from xmtp import Client
from xmtp.signers import create_signer
from xmtp.types import ClientOptions, LogLevel
from xmtp_agent.name_resolver import create_name_resolver

recipient = "deanpierce.eth"


async def main() -> None:
    signer = create_signer(private_key)
    client = await Client.create(
        signer,
        ClientOptions(
            env="dev",
            disable_history_sync=True,
            logging_level=LogLevel.WARN,
        ),
    )

    resolver = create_name_resolver()
    address = await resolver(recipient)
    if address is None:
        raise ValueError(f'Could not resolve address for "{recipient}"')

    dm = await client.conversations.new_dm(address)
    await dm.send(f"Hello from xmtp-py {xmtp.__version__}")


if __name__ == "__main__":
    asyncio.run(main())
```

## Key management tips

- `create_user()` generates an in-memory key; persist the private key yourself if you want a stable inbox across restarts.
- Prefer `XMTP_WALLET_KEY` and `XMTP_DB_ENCRYPTION_KEY` in environment variables or a secrets manager; never commit them to git.
- Use a stable `db_path` and keep the database directory between runs. Losing it creates a new installation and can hit installation limits.
- For production, wrap a hardware wallet or KMS signer by implementing the `Signer` protocol instead of storing raw keys.

## Configuration & troubleshooting

History sync is disabled by default. To enable it, pass
`disable_history_sync=False` (and optionally `history_sync_url`) in `ClientOptions`,
or set `XMTP_HISTORY_SYNC_URL`.

Endpoint overrides via env:
- `XMTP_ENV`, `XMTP_API_URL`, `XMTP_HISTORY_SYNC_URL`, `XMTP_GATEWAY_HOST`
- `XMTP_DISABLE_HISTORY_SYNC=1` to force identity calls through the primary API

Common issues:
- **Missing `libxmtpv3.so`**: install will build it automatically, but you need Rust (`cargo`) and `git`. See `bindings/python/README.md` for overrides. If no prebuilt wheels are available, `pip install xmtp` will fall back to a source build.
- **History sync gRPC errors**: leave history sync disabled, or set `XMTP_DISABLE_HISTORY_SYNC=1`. To re-enable, pass `disable_history_sync=False` (and optionally `history_sync_url`) in `ClientOptions`.
- **Gateway host errors**: `gateway_host` is an advanced setting; only set it when you have a gateway that supports XMTP gRPC.

## LibXMTP bindings

This SDK uses [libxmtp](https://github.com/xmtp/libxmtp) Python bindings for core XMTP functionality including cryptography, networking, and protocol implementation.

**Minimum version**: 1.7.0-r3

## Documentation

- [XMTP Documentation](https://docs.xmtp.org/)
- [Build an XMTP Agent](https://docs.xmtp.org/agents/get-started/build-an-agent)

## Contributing

See [CONTRIBUTING.md](./CONTRIBUTING.md) for guidelines.

## License

Apache 2.0

### sdks/python-sdk/README.md

# xmtp-py (python-sdk)

Unofficial Python SDK for the XMTP network.

This package mirrors the xmtp-js node SDK, adapted to Python idioms.

## Quick start

```python
from xmtp import Client
from xmtp.signers import create_signer
from xmtp.types import ClientOptions

signer = create_signer('0xYOUR_PRIVATE_KEY')
client = await Client.create(signer, ClientOptions(env='dev', disable_history_sync=True))

dm = await client.conversations.new_dm('0x...')
await dm.send('Hello from Python!')
```

## Key management tips

- `create_signer()` expects a long-lived private key; store it outside source control.
- `XMTP_WALLET_KEY` can be used to load a signer from the environment.
- Keep `XMTP_DB_ENCRYPTION_KEY` and the database path stable to preserve installations.

## Configuration & troubleshooting

Endpoint overrides via env: `XMTP_API_URL`, `XMTP_HISTORY_SYNC_URL`, `XMTP_GATEWAY_HOST`, `XMTP_DISABLE_HISTORY_SYNC=1`.

History sync is disabled by default. Set `disable_history_sync=False` (and optionally
`history_sync_url`) to enable it.

If you see history sync gRPC errors, leave history sync disabled or set
`XMTP_DISABLE_HISTORY_SYNC=1`.

### sdks/agent-sdk/README.md

# xmtp-agent-sdk

Unofficial XMTP agent SDK for Python.

This package provides an event-driven agent framework inspired by xmtp-js.

## Quick start

```python
from xmtp_agent import Agent
from xmtp_agent.user import create_signer, create_user

user = create_user()
signer = create_signer(user)
agent = await Agent.create(signer)

@agent.on('text')
async def on_text(ctx):
    await ctx.send_text('Hello from my agent!')

await agent.start()
```

## Key management tips

- `create_user()` generates a fresh private key each run; persist the key if you want a stable inbox.
- `Agent.create_from_env()` loads `XMTP_WALLET_KEY`; prefer env vars or a secrets manager over hardcoding.
- Keep your database directory and encryption key stable to avoid spinning up excess installations.

## Configuration & troubleshooting

Endpoint overrides via env: `XMTP_API_URL`, `XMTP_HISTORY_SYNC_URL`, `XMTP_GATEWAY_HOST`, `XMTP_DISABLE_HISTORY_SYNC=1`.

History sync is disabled by default. Set `disable_history_sync=False` (and optionally
`history_sync_url`) to enable it.

If you see history sync gRPC errors, leave history sync disabled or set
`XMTP_DISABLE_HISTORY_SYNC=1`.

### docs/index.rst

xmtp-py Documentation
=====================

This documentation is generated from code docstrings.

.. toctree::
   :maxdepth: 2
   :caption: SDKs

   python_sdk
   agent_sdk

.. toctree::
   :maxdepth: 1
   :caption: Guides

   migration_guide
   examples
   contributing
   test_parity

### docs/python_sdk.rst

Python SDK
==========

Key management tips
-------------------

- Store private keys outside source control (env vars or a secrets manager).
- Keep ``XMTP_DB_ENCRYPTION_KEY`` and your database path stable between runs.
- Use a custom signer implementation for hardware wallets or KMS.

Configuration & troubleshooting
-------------------------------

- Override endpoints with ``XMTP_API_URL``, ``XMTP_HISTORY_SYNC_URL``, or ``XMTP_GATEWAY_HOST``.
- History sync is disabled by default. Set ``disable_history_sync=False`` (and optionally
  ``history_sync_url``) to enable it.
- Set ``XMTP_DISABLE_HISTORY_SYNC=1`` if the history sync endpoint returns gRPC errors.

.. automodule:: xmtp.client
   :members:
   :undoc-members:

.. automodule:: xmtp.conversations
   :members:
   :undoc-members:

.. automodule:: xmtp.conversation
   :members:
   :undoc-members:

.. automodule:: xmtp.messages
   :members:
   :undoc-members:

.. automodule:: xmtp.preferences
   :members:
   :undoc-members:

.. automodule:: xmtp.types
   :members:
   :undoc-members:

.. automodule:: xmtp.signers
   :members:
   :undoc-members:

.. automodule:: xmtp.env
   :members:
   :undoc-members:

### docs/agent_sdk.rst

Agent SDK
=========

Key management tips
-------------------

- ``create_user()`` generates a new in-memory key; persist it if you want a stable inbox.
- ``XMTP_WALLET_KEY`` should be provided via env vars or a secrets manager.
- Preserve the database directory and encryption key to avoid extra installations.

Configuration & troubleshooting
-------------------------------

- Override endpoints with ``XMTP_API_URL``, ``XMTP_HISTORY_SYNC_URL``, or ``XMTP_GATEWAY_HOST``.
- History sync is disabled by default. Set ``disable_history_sync=False`` (and optionally
  ``history_sync_url``) to enable it.
- Set ``XMTP_DISABLE_HISTORY_SYNC=1`` if the history sync endpoint returns gRPC errors.

.. automodule:: xmtp_agent.agent
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.context
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.filters
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.middleware
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.command_router
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.user
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.name_resolver
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.attachments
   :members:
   :undoc-members:

.. automodule:: xmtp_agent.debug
   :members:
   :undoc-members:

### docs/examples.rst

Examples
========

Python SDK
----------

.. code-block:: python

   import asyncio

   from xmtp import Client
   from xmtp.signers import create_signer
   from xmtp.types import ClientOptions


   async def main() -> None:
       signer = create_signer('0xYOUR_PRIVATE_KEY')
       options = ClientOptions(env='dev')
       client = await Client.create(signer, options)

       dm = await client.conversations.new_dm('0x...')
       await dm.send('Hello from Python!')


   asyncio.run(main())

Agent SDK
---------

.. code-block:: python

   import asyncio

   from xmtp_agent import Agent
   from xmtp_agent.user import create_signer, create_user


   async def main() -> None:
       user = create_user()
       signer = create_signer(user)
       agent = await Agent.create(signer)

       @agent.on('text')
       async def handle_text(ctx):
           await ctx.send_text('Hello from my XMTP Agent!')

       await agent.start()


   asyncio.run(main())

### docs/migration_guide.rst

Migration Guide (xmtp-js -> xmtp-py)
===================================

This SDK mirrors the xmtp-js APIs with Python-idiomatic conventions.

Key differences
---------------

- JavaScript ``camelCase`` becomes Python ``snake_case``.
- Async APIs use ``async``/``await`` and ``pytest-asyncio`` for tests.
- Options are passed as ``ClientOptions`` rather than plain objects.

Common mappings
---------------

+-----------------------------+----------------------------------+
| xmtp-js                     | xmtp-py                          |
+=============================+==================================+
| ``Client.create(...)``      | ``await Client.create(...)``     |
+-----------------------------+----------------------------------+
| ``newDm()``                 | ``await new_dm()``               |
+-----------------------------+----------------------------------+
| ``streamAllMessages()``     | ``stream_all_messages()``        |
+-----------------------------+----------------------------------+
| ``inboxId``                 | ``inbox_id``                     |
+-----------------------------+----------------------------------+
| ``ContentTypeId``           | ``ContentTypeId``                |
+-----------------------------+----------------------------------+

### docs/contributing.rst

Contributing
============

See the repository's ``CONTRIBUTING.md`` for setup, testing, and linting notes.

Testing and coverage
--------------------

This repo enforces full branch coverage for the Python SDKs and content types.
We exclude generated UniFFI glue (``xmtp_bindings/xmtpv3.py``) from coverage
because it is machine-generated, but we still exercise bindings via smoke tests.

Run tests with coverage locally:

.. code-block:: bash

   pytest \\
     --cov=sdks/python-sdk/src \\
     --cov=sdks/agent-sdk/src \\
     --cov=content-types \\
     --cov=bindings/python/src \\
     --cov-branch \\
     --cov-report=term-missing:skip-covered \\
     --cov-report=xml \\
     --cov-report=html \\
     --cov-config=.coveragerc

FFI/Rust coverage (libxmtp)
---------------------------

Rust coverage lives in the libxmtp repo, not this one. To measure it, run
``cargo llvm-cov`` in the libxmtp workspace (example):

.. code-block:: bash

   cargo llvm-cov --workspace --all-features --lcov --output-path lcov.info

### bindings/python/README.md

# xmtp-bindings

Python bindings for libxmtp (XMTP v3) generated via UniFFI.

## Install-time build

The Python package now builds the `libxmtp` native library during install. This requires:

- Rust toolchain (`cargo`)
- `git`

Environment overrides:

- `XMTP_LIBXMTP_PATH`: use an existing libxmtp checkout instead of cloning
- `XMTP_LIBXMTP_REF`: git ref (tag/branch/commit) to checkout
- `XMTP_LIBXMTP_REPO`: override the libxmtp git URL
- `XMTP_BINDINGS_SKIP_BUILD=1`: skip native build (requires a prebuilt `libxmtpv3` next to `xmtpv3.py`)
- `XMTP_BINDINGS_FORCE_BUILD=1`: rebuild even if a native library already exists

If `XMTP_LIBXMTP_REF` is not set, builds default to the pinned ref stored in
`src/xmtp_bindings/libxmtp.ref`.

During builds, the UniFFI Python bindings (`xmtpv3.py`) are regenerated from the
same libxmtp checkout used to build the native library to keep the wrapper and
shared library in sync.

## Manual build

These bindings are generated from the `libxmtp` Rust workspace. To regenerate manually:

```bash
# Clone libxmtp into .deps if needed
mkdir -p .deps
[ -d .deps/libxmtp ] || git clone --depth 1 https://github.com/xmtp/libxmtp .deps/libxmtp

# Build the native library
cd .deps/libxmtp
cargo build -p xmtpv3 --release

# Generate Python bindings
cd bindings_ffi
cargo run --bin ffi-uniffi-bindgen --release --features uniffi/cli generate \
  --library ../target/release/libxmtpv3.so \
  --out-dir ../../../bindings/python/src/xmtp_bindings \
  --language python

# Copy the shared library next to the generated module
cp ../target/release/libxmtpv3.so ../../../bindings/python/src/xmtp_bindings/
```

## Notes

- The generated `xmtpv3.py` expects `libxmtpv3` to sit next to it.
- This package is intended to be consumed by the higher-level `python-sdk`.

### content-types/content-type-group-updated/README.md

# xmtp-content-type-group-updated

Group updated content type implementation for XMTP Python SDKs.

### content-types/content-type-markdown/README.md

# xmtp-content-type-markdown

Markdown content type codec for XMTP.

### content-types/content-type-primitives/README.md

# xmtp-content-type-primitives

Shared content type primitives for XMTP Python SDKs.

### content-types/content-type-reaction/README.md

# xmtp-content-type-reaction

Reaction content type codec for XMTP.

### content-types/content-type-read-receipt/README.md

# xmtp-content-type-read-receipt

Read receipt content type codec for XMTP.

### content-types/content-type-remote-attachment/README.md

# xmtp-content-type-remote-attachment

Attachment and remote attachment content types for XMTP Python SDKs.

### content-types/content-type-reply/README.md

# xmtp-content-type-reply

Reply content type implementation for XMTP Python SDKs.

### content-types/content-type-text/README.md

# xmtp-content-type-text

Text content type codec for XMTP.

### content-types/content-type-transaction-reference/README.md

# xmtp-content-type-transaction-reference

Transaction reference content type implementation for XMTP Python SDKs.

### content-types/content-type-wallet-send-calls/README.md

# xmtp-content-type-wallet-send-calls

Wallet send calls content type implementation for XMTP Python SDKs.

## Python SDK API Reference

### Module: xmtp

Doc: XMTP Python SDK (unofficial).

Exports: Client, ClientOptions, Conversation, Conversations, DecodedMessage, Dm, Group, Identifier, IdentifierKind, LogLevel, load_client_options_from_env, load_signer_from_env, Preferences, XmtpEnv

### Module: xmtp.async_stream

Doc: Async stream helper.

Constants:
- T = TypeVar('T')

Classes:
- class AsyncStream(AsyncIterator[T], Generic[T]): Async stream backed by an asyncio queue.
  - async close(self) -> None: Close the underlying stream if possible.

### Module: xmtp.bindings

Doc: Bindings loader for libxmtp (UniFFI).

Exports: NativeBindings

### Module: xmtp.client

Doc: XMTP client entry point.

Constants:
- ContentT = TypeVar('ContentT')

Classes:
- class Client: Main client for interacting with the XMTP network.
  - async create(cls, signer: Signer, options: ClientOptions | None = None) -> Client: Create a client with a signer.
  - async create_from_env(cls, options: ClientOptions | None = None) -> Client: Create a client using XMTP environment variables.
  - async from_env(cls, options: ClientOptions | None = None) -> Client: Alias for create_from_env.
  - async build(cls, identifier: Identifier, options: ClientOptions | None = None) -> Client: Create a client with an identifier (no signer).
  - inbox_id(self) -> str | None: Inbox identifier for the user.
  - installation_id(self) -> bytes | None: Installation identifier for the user.
  - account_identifier(self) -> Identifier | None: Return the account identifier used to initialize the client.
  - is_registered(self) -> bool: Return True if the user is registered with XMTP.
  - conversations(self) -> Conversations: Conversation manager for the client.
  - options(self) -> ClientOptions: Return the client options.
  - preferences(self) -> Preferences: Preferences manager for the client.
  - async register(self) -> None: Register the user on XMTP.
  - async can_message(self, identifiers: list[Identifier]) -> dict[str, bool]: Return a map of identifiers to messageability.
  - async get_inbox_id_by_identifier(self, identifier: Identifier) -> str | None: Resolve an identifier to an inbox id.
  - register_codec(self, codec: ContentCodec[ContentT]) -> None: Register a content codec for encoding/decoding.
  - register_codecs(self, codecs: Sequence[ContentCodec[ContentT]]) -> None: Register multiple content codecs.
  - codec_for(self, content_type: ContentTypeId | str) -> ContentCodec[object] | None: Return the codec for a content type, if registered.
  - encode_content(self, content: ContentT, content_type: ContentTypeId | str) -> bytes: Encode content for sending.
  - prepare_for_send(self, content: ContentT, content_type: ContentTypeId | str) -> tuple[bytes, NativeBindings.FfiSendMessageOpts | _SendMessageOpts]: Prepare content for sending with codec-derived send options.

### Module: xmtp.constants

Doc: XMTP network constants.

Constants:
- API_URLS = {'local': 'http://localhost:5556', 'dev': 'https://grpc.dev.xmtp.network:443', 'production': 'https://grpc.production...
- HISTORY_SYNC_URLS = {'local': 'http://localhost:5558', 'dev': 'https://message-history.dev.ephemera.network', 'production': 'https://mess...

### Module: xmtp.conversation

Doc: Conversation models.

Classes:
- class Conversation: Base conversation class.
  - id(self) -> bytes: Conversation identifier bytes.
  - consent_state(self) -> NativeBindings.FfiConsentState | None: Return the conversation consent state if available.
  - async update_consent_state(self, state: NativeBindings.FfiConsentState) -> None: Update the conversation consent state.
  - async send(self, content: object, content_type: ContentTypeId | str | None = None) -> bytes: Send a message in the conversation.
- class Dm(Conversation): Direct message conversation.
  - peer_inbox_id(self) -> str | None: Inbox ID of the DM peer, if available.
- class Group(Conversation): Group conversation.
  - async add_members(self, inbox_ids: list[str]) -> None: Add members to the group by inbox ID.
  - async remove_members(self, inbox_ids: list[str]) -> None: Remove members from the group by inbox ID.
  - async add_members_by_identifiers(self, identifiers: list[Identifier]) -> None: Add members to the group by identifier.
  - async remove_members_by_identifiers(self, identifiers: list[Identifier]) -> None: Remove members from the group by identifier.
  - async members(self) -> list[str]: Return group member inbox IDs.
  - async add_admin(self, inbox_id: str) -> None: Add an admin to the group.
  - async remove_admin(self, inbox_id: str) -> None: Remove an admin from the group.
  - async is_admin(self, inbox_id: str) -> bool: Return True if inbox_id is an admin.
  - async is_super_admin(self, inbox_id: str) -> bool: Return True if inbox_id is a super admin.
  - name(self) -> str | None: Group name.
  - description(self) -> str | None: Group description.
  - image_url(self) -> str | None: Group image URL.

### Module: xmtp.conversations

Doc: Conversation management.

Classes:
- class ListConversationsOptions: Options for listing conversations.
  - to_ffi(self) -> NativeBindings.FfiListConversationsOptions
- class Conversations: Manage XMTP conversations.
  - async new_dm(self, address: str) -> Dm: Create a new direct message conversation by address.
  - async new_dm_with_identifier(self, identifier: Identifier) -> Dm: Create a new direct message using an identifier.
  - async new_group(self, members: list[str]) -> Group: Create a new group conversation by member addresses.
  - async new_group_with_identifiers(self, identifiers: list[Identifier]) -> Group: Create a new group conversation using identifiers.
  - async list(self, options: ListConversationsOptions | None = None) -> builtins.list[Conversation]: List all conversations.
  - async list_dms(self, options: ListConversationsOptions | None = None) -> builtins.list[Dm]: List direct message conversations.
  - async list_groups(self, options: ListConversationsOptions | None = None) -> builtins.list[Group]: List group conversations.
  - async get_conversation_by_id(self, conversation_id: bytes) -> Conversation | None: Return a conversation by identifier.
  - stream(self) -> AsyncStream[Conversation | NativeBindings.FfiSubscribeError]: Stream new conversations.
  - stream_all_messages(self) -> AsyncStream[DecodedMessage[object] | NativeBindings.FfiSubscribeError]: Stream all messages across conversations.
  - async sync(self) -> None: Sync new conversations.
  - async sync_all_conversations(self, consent_states: builtins.list[NativeBindings.FfiConsentState] | None = None) -> None: Sync all conversations.

### Module: xmtp.env

Doc: Environment variable helpers for XMTP.

Exports: load_client_options_from_env, load_signer_from_env

Functions:
- load_client_options_from_env(options: ClientOptions | None = None) -> ClientOptions: Load client options overrides from environment variables.
- load_signer_from_env() -> Signer: Create an EOA signer from XMTP_WALLET_KEY.

### Module: xmtp.errors

Doc: Error types for XMTP.

Classes:
- class XmtpError(Exception): Base class for XMTP errors.
- class CodecNotFoundError(XmtpError): Raised when a codec is missing for a content type.
- class MissingContentTypeError(XmtpError): Raised when content type is required but missing.
- class SignerUnavailableError(XmtpError): Raised when a signer is required but not provided.
- class ClientNotInitializedError(XmtpError): Raised when client is used before initialization.
- class AccountAlreadyAssociatedError(XmtpError): Raised when account is already associated with an inbox.
- class InboxReassignError(XmtpError): Raised when inbox reassignment is disallowed.
- class StreamFailedError(XmtpError): Raised when a stream fails after retries.
- class StreamInvalidRetryAttemptsError(XmtpError): Raised when retry attempts are invalid.
- class NotImplementedXmtpError(XmtpError): Raised when a feature has not been implemented yet.
- class InvalidGroupMembershipChangeError(XmtpError): Raised when group membership changes are invalid.
- class DatabaseOpenError(XmtpError): Raised when the local XMTP database cannot be opened.

### Module: xmtp.identifiers

Doc: Identifier types for XMTP.

Classes:
- class IdentifierKind(str, Enum): Supported identifier kinds.
- class Identifier: Identifier for XMTP inbox lookups.

### Module: xmtp.messages

Doc: Message models for XMTP.

Constants:
- ContentT = TypeVar('ContentT')

Classes:
- class DecodedMessage(Generic[ContentT]): Decoded message representation.

### Module: xmtp.preferences

Doc: User preferences management.

Classes:
- class Preferences: Manage user preferences for the XMTP client.
  - async refresh(self) -> None: Refresh preferences from the network.
  - async inbox_state(self, refresh_from_network: bool = False) -> NativeBindings.FfiInboxState: Return the current inbox state.
  - async get_latest_inbox_state(self, inbox_id: str) -> NativeBindings.FfiInboxState: Return the latest inbox state for a specific inbox.
  - async inbox_state_from_inbox_ids(self, inbox_ids: list[str], refresh_from_network: bool = False) -> list[NativeBindings.FfiInboxState]: Return inbox state for the specified inbox IDs.
  - async set_consent_states(self, records: list[NativeBindings.FfiConsent]) -> None: Update consent states for multiple records.
  - async get_consent_state(self, entity_type: NativeBindings.FfiConsentEntityType, entity: str) -> NativeBindings.FfiConsentState: Get consent state for a specific entity.

### Module: xmtp.signers

Doc: Signer helpers for XMTP.

Exports: Signer, SignerType, EoaSigner, ScwSigner, create_signer

### Module: xmtp.signers.base

Doc: Signer protocol for XMTP.

Classes:
- class SignerType(str, Enum): Supported signer types.
- class Signer(Protocol): Signer interface for XMTP clients.
  - async get_address(self) -> str: Return the wallet address for this signer.
  - async get_identifier(self) -> Identifier: Return the XMTP identifier for this signer.
  - async sign_message(self, message: bytes) -> bytes: Sign a message and return the signature bytes.
  - async get_chain_id(self) -> int: Return the chain ID (SCW only).
  - async get_block_number(self) -> int | None: Return the block number for SCW signatures, if needed.

### Module: xmtp.signers.eoa

Doc: Externally owned account signer.

Classes:
- class EoaSigner(Signer): EOA signer created from a private key.
  - async get_address(self) -> str: Return the wallet address for this signer.
  - async get_identifier(self) -> Identifier: Return the XMTP identifier for this signer.
  - async sign_message(self, message: bytes) -> bytes: Sign a message and return the signature bytes.
  - async get_chain_id(self) -> int: Return the chain ID (not applicable for EOA).
  - async get_block_number(self) -> int | None: Return block number (not applicable for EOA).

Functions:
- create_signer(private_key: str) -> EoaSigner: Create an EOA signer from a private key string.

### Module: xmtp.signers.scw

Doc: Smart contract wallet signer.

Classes:
- class ScwSigner(Signer): Signer for smart contract wallets.
  - async get_address(self) -> str: Return the wallet address for this signer.
  - async get_identifier(self) -> Identifier: Return the XMTP identifier for this signer.
  - async sign_message(self, message: bytes) -> bytes: Sign a message and return the signature bytes.
  - async get_chain_id(self) -> int: Return the chain ID for SCW signatures.
  - async get_block_number(self) -> int | None: Return the block number for SCW signatures, if any.

### Module: xmtp.types

Doc: Shared XMTP type definitions.

Constants:
- XmtpEnv = Literal['local', 'dev', 'production']

Classes:
- class LogLevel(str, Enum): Logging levels for libxmtp.
- class ClientOptions: Options for constructing an XMTP client.
  - resolved_api_url(self) -> str: Return the API URL based on env and overrides.
  - resolved_history_sync_url(self) -> str | None: Return the history sync URL based on env and overrides.

### Module: xmtp.utils

Doc: Utility helpers for XMTP.

Functions:
- is_hex_string(value: str, length: int | None = None) -> bool: Return True if value is a hex string.
- hex_to_bytes(value: str) -> bytes: Convert a hex string (with or without 0x prefix) to bytes.
- coerce_db_encryption_key(value: bytes | str | None) -> bytes | None: Normalize the database encryption key to bytes if provided.

## Agent SDK API Reference

### Module: xmtp_agent

Doc: XMTP agent SDK (unofficial).

Exports: Agent, backoff_reconnect, ClientContext, ConversationContext, CommandRouter, MessageContext, MockAsyncStream, ResolvedRecipient, create_identifier, create_mock_message, create_name_resolver, create_signer, create_user, deserialize_message, download_remote_attachment, filter, flush_asyncio, get_installation_info, get_test_url, log_details, record_messages, replay_messages, resolve_recipient, serialize_message

### Module: xmtp_agent.agent

Doc: Agent class for XMTP.

Constants:
- EventPayload = ClientContext | ConversationContext | MessageContext | Exception
- EventHandler = Callable[[EventPayload], Awaitable[None] | None]

Classes:
- class Agent: Main agent class with an event-driven API.
  - async create(cls, signer: Signer, options: ClientOptions | None = None) -> Agent: Create an agent with a signer.
  - async create_from_env(cls, options: ClientOptions | None = None) -> Agent: Create an agent from environment variables.
  - on(self, event: str, handler: EventHandler | None = None) -> EventHandler | Callable[[EventHandler], EventHandler]: Register an event handler.
  - use(self, *middlewares: Middleware | list[Middleware]) -> Agent: Register middleware for message handling.
  - errors(self) -> _ErrorRegistrar: Return an object to register error middleware.
  - client(self) -> Client
  - async start(self) -> None: Start the agent.
  - async stop(self) -> None: Stop the agent.

### Module: xmtp_agent.attachments

Doc: Attachment utilities for agents.

Exports: download_remote_attachment

Functions:
- async download_remote_attachment(remote_attachment: RemoteAttachment) -> bytes: Download a remote attachment and verify its digest.

### Module: xmtp_agent.command_router

Doc: Command router middleware for XMTP agents.

Exports: CommandRouter

Classes:
- class CommandRouter: Route slash commands to handlers for text messages.
  - command_list(self) -> list[str]
  - command(self, command: str, handler: Handler) -> CommandRouter
  - default(self, handler: Handler) -> CommandRouter
  - async handle(self, ctx: MessageContext) -> bool
  - middleware(self) -> Middleware

### Module: xmtp_agent.context

Doc: Context objects for agent handlers.

Classes:
- class ClientContext: Context passed to lifecycle handlers.
  - client(self) -> Client
  - get_client_address(self) -> str | None
- class ConversationContext(ClientContext): Context passed to conversation handlers.
  - is_dm(self) -> bool
  - is_group(self) -> bool
  - async send_markdown(self, markdown: str) -> None
  - async send_text(self, text: str) -> None
  - async send_remote_attachment(self, attachment: RemoteAttachment) -> None
  - conversation(self) -> Conversation
  - is_allowed(self) -> bool
  - is_denied(self) -> bool
  - is_unknown(self) -> bool
- class MessageContext(ConversationContext): Context passed to message handlers.
  - uses_codec(self, codec_class: type[ContentCodec[object]]) -> bool
  - is_markdown(self) -> bool
  - is_text(self) -> bool
  - is_reply(self) -> bool
  - is_reaction(self) -> bool
  - is_remote_attachment(self) -> bool
  - async send_reaction(self, content: str, schema: ReactionSchema = ReactionSchema.UNICODE) -> None
  - async send_text_reply(self, text: str) -> None
  - async send_markdown_reply(self, markdown: str) -> None
  - async get_sender_address(self) -> str | None
  - message(self) -> DecodedMessage[object]

### Module: xmtp_agent.debug

Doc: Debug helpers for agent development.

Exports: get_test_url, get_installation_info, log_details, InstallationInfo

Classes:
- class InstallationInfo: Installation details for an XMTP client.

Functions:
- get_test_url(client: Client) -> str: Return a URL to test the agent on xmtp.chat.
- async get_installation_info(client: Client) -> InstallationInfo: Return information about the client's installation state.
- async log_details(agent: Agent) -> None: Log basic agent details for debugging.

### Module: xmtp_agent.errors

Doc: Agent SDK errors.

Classes:
- class AgentError(Exception): Base error for the agent SDK.
- class AgentStreamingError(AgentError): Error raised when streaming fails.

### Module: xmtp_agent.filters

Doc: Message filter helpers.

Exports: filter, f, from_self, has_content, is_dm, is_group, is_group_update, is_markdown, is_reaction, is_read_receipt, is_remote_attachment, is_reply, is_text, is_transaction_reference, is_wallet_send_calls, uses_codec

Constants:
- filter = _Filter()
- f = filter

Functions:
- from_self(message: DecodedMessage[Any], client: Client) -> bool
- has_content(message: DecodedMessage[Any]) -> bool
- is_dm(conversation: Conversation) -> bool
- is_group(conversation: Conversation) -> bool
- is_group_update(message: DecodedMessage[Any]) -> bool
- is_markdown(message: DecodedMessage[Any]) -> bool
- is_reaction(message: DecodedMessage[Any]) -> bool
- is_read_receipt(message: DecodedMessage[Any]) -> bool
- is_remote_attachment(message: DecodedMessage[Any]) -> bool
- is_reply(message: DecodedMessage[Any]) -> bool
- is_text(message: DecodedMessage[Any]) -> bool
- is_transaction_reference(message: DecodedMessage[Any]) -> bool
- is_wallet_send_calls(message: DecodedMessage[Any]) -> bool
- uses_codec(message: DecodedMessage[Any], codec_class: type[ContentCodec[Any]]) -> bool

### Module: xmtp_agent.middleware

Doc: Middleware interfaces for the agent SDK.

Exports: ErrorMiddleware, ErrorNextHandler, Handler, LifecycleHandler, Middleware, NextHandler, ConversationHandler, backoff_reconnect

Constants:
- Handler = Callable[[MessageContext], Awaitable[None]]
- ConversationHandler = Callable[[ConversationContext], Awaitable[None]]
- LifecycleHandler = Callable[[ClientContext], Awaitable[None]]
- NextHandler = Callable[[], Awaitable[None]]
- ErrorNextHandler = Callable[[Exception | None], Awaitable[None]]
- Middleware = Callable[[MessageContext, NextHandler], Awaitable[None]]
- ErrorMiddleware = Callable[[Exception, MessageContext | ClientContext, ErrorNextHandler], Awaitable[None]]

Functions:
- backoff_reconnect(*, initial_delay: float = 0.5, max_delay: float = 30.0, multiplier: float = 2.0, reset_after: float = 60.0) -> ErrorMiddleware: Return error middleware that handles stream errors with backoff.

### Module: xmtp_agent.name_resolver

Doc: Name resolution utilities.

Exports: create_name_resolver

Functions:
- create_name_resolver(api_key: str | None = None) -> Callable[[str], Awaitable[str | None]]: Create an async name resolver backed by web3.bio.

### Module: xmtp_agent.recipient_resolver

Doc: Recipient resolution helpers for agents.

Exports: ResolvedRecipient, resolve_recipient

Classes:
- class ResolvedRecipient: Resolved recipient details.

Functions:
- async resolve_recipient(client: Client, recipient: str | Identifier, *, name_resolver: Callable[[str], Awaitable[str | None]] | None = None, consent_state: NativeBindings.FfiConsentState | None = None) -> ResolvedRecipient: Resolve an inbox ID or address for a recipient.

### Module: xmtp_agent.test_utils

Doc: Testing utilities for agent development.

Exports: MockAsyncStream, create_mock_message, deserialize_message, flush_asyncio, record_messages, replay_messages, serialize_message

Classes:
- class MockAsyncStream(AsyncStream[ContentT], Generic[ContentT]): Async stream helper with push/end for tests.
  - push(self, value: ContentT) -> None
  - end(self) -> None
  - async close(self) -> None

Functions:
- create_mock_message(content: ContentT, **overrides: object) -> DecodedMessage[ContentT]: Create a decoded message with sensible defaults for tests.
- serialize_message(message: DecodedMessage[Any]) -> dict[str, Any]: Serialize a decoded message to a JSON-friendly dict.
- deserialize_message(payload: dict[str, Any]) -> DecodedMessage[Any]: Deserialize a decoded message from a JSON-friendly dict.
- async record_messages(stream: AsyncStream[DecodedMessage[Any]], limit: int | None = None) -> list[dict[str, Any]]: Record decoded messages from a stream.
- replay_messages(records: Iterable[dict[str, Any]]) -> MockAsyncStream[DecodedMessage[Any]]: Replay recorded messages as an async stream.
- async flush_asyncio() -> None: Flush the asyncio event loop for tests.

### Module: xmtp_agent.user

Doc: User helpers for XMTP agents.

Exports: User, create_user, create_signer, create_identifier

Classes:
- class User: User wallet wrapper.

Functions:
- create_user(private_key: str | None = None) -> User: Create a user from an optional private key.
- create_signer(user_or_key: User | str) -> EoaSigner: Create an EOA signer from a user or private key.
- create_identifier(user: User) -> Identifier: Create an XMTP identifier for a user.

## Content Types API Reference

### Module: xmtp_content_type_group_updated

Doc: Group updated content type for XMTP.

Exports: ContentTypeGroupUpdated, GroupUpdated, GroupUpdatedCodec

Constants:
- ContentTypeGroupUpdated = ContentTypeId(authority_id='xmtp.org', type_id='group_updated', version_major=1, version_minor=0)

Classes:
- class GroupUpdatedCodec(ContentCodec[GroupUpdated]): Codec for group updated messages.
  - content_type(self) -> ContentTypeId
  - encode(self, content: GroupUpdated, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> GroupUpdated
  - fallback(self, content: GroupUpdated) -> str | None
  - should_push(self, content: GroupUpdated) -> bool

### Module: xmtp_content_type_markdown

Doc: Markdown content type for XMTP.

Exports: ContentTypeMarkdown, Encoding, MarkdownCodec

Constants:
- ContentTypeMarkdown = ContentTypeId(authority_id='xmtp.org', type_id='markdown', version_major=1, version_minor=0)

Classes:
- class Encoding(str, Enum)
- class MarkdownCodec(ContentCodec[str]): Codec for markdown messages.
  - content_type(self) -> ContentTypeId
  - encode(self, content: str, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> str
  - fallback(self, content: str) -> str | None
  - should_push(self, content: str) -> bool

### Module: xmtp_content_type_primitives

Doc: Content type primitives for XMTP.

Exports: BaseContentCodec, CodecRegistry, ContentCodec, ContentTypeId, EncodedContent, content_type_from_string, content_type_to_string, content_types_are_equal

Classes:
- class ContentTypeId: Content type identifier.
  - same_as(self, other: ContentTypeId) -> bool
- class EncodedContent: Encoded content payload.
- class CodecRegistry(Protocol): Registry for content codecs.
  - codec_for(self, content_type: ContentTypeId) -> ContentCodec[Any] | None
- class ContentCodec(Protocol[TContent]): Protocol for content codecs.
  - content_type(self) -> ContentTypeId
  - encode(self, content: TContent, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> TContent
  - fallback(self, content: TContent) -> str | None
  - should_push(self, content: TContent) -> bool
- class BaseContentCodec(Generic[TContent]): Base class for content codecs with default behaviors.
  - content_type(self) -> ContentTypeId
  - encode(self, content: TContent, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> TContent
  - fallback(self, content: TContent) -> str | None
  - should_push(self, content: TContent) -> bool

Functions:
- content_types_are_equal(a: ContentTypeId, b: ContentTypeId) -> bool: Return True if content type IDs are equal.
- content_type_to_string(content_type: ContentTypeId) -> str: Convert content type ID to string form.
- content_type_from_string(content_type_string: str) -> ContentTypeId: Parse content type ID from string form.

### Module: xmtp_content_type_reaction

Doc: Reaction content type for XMTP.

Exports: ContentTypeReaction, Reaction, ReactionAction, ReactionSchema, ReactionCodec

Constants:
- ContentTypeReaction = ContentTypeId(authority_id='xmtp.org', type_id='reaction', version_major=1, version_minor=0)

Classes:
- class ReactionAction(str, Enum)
- class ReactionSchema(str, Enum)
- class Reaction
- class ReactionCodec(ContentCodec[Reaction]): Codec for reaction messages.
  - content_type(self) -> ContentTypeId
  - encode(self, content: Reaction, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> Reaction
  - fallback(self, content: Reaction) -> str | None
  - should_push(self, content: Reaction) -> bool

### Module: xmtp_content_type_read_receipt

Doc: Read receipt content type for XMTP.

Exports: ContentTypeReadReceipt, ReadReceipt, ReadReceiptCodec

Constants:
- ContentTypeReadReceipt = ContentTypeId(authority_id='xmtp.org', type_id='readReceipt', version_major=1, version_minor=0)
- ReadReceipt = dict[str, object]

Classes:
- class ReadReceiptCodec(ContentCodec[ReadReceipt]): Codec for read receipt messages.
  - content_type(self) -> ContentTypeId
  - encode(self, content: ReadReceipt, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> ReadReceipt
  - fallback(self, content: ReadReceipt) -> str | None
  - should_push(self, content: ReadReceipt) -> bool

### Module: xmtp_content_type_remote_attachment

Doc: Attachment content types for XMTP.

Exports: Attachment, AttachmentCodec, ContentTypeAttachment, ContentTypeRemoteAttachment, RemoteAttachment, RemoteAttachmentCodec

Constants:
- ContentTypeAttachment = ContentTypeId(authority_id='xmtp.org', type_id='attachment', version_major=1, version_minor=0)
- ContentTypeRemoteAttachment = ContentTypeId(authority_id='xmtp.org', type_id='remoteStaticAttachment', version_major=1, version_minor=0)

Classes:
- class Attachment: Attachment payload.
- class RemoteAttachment: Remote attachment metadata.
- class AttachmentCodec(ContentCodec[Attachment]): Codec for attachment payloads.
  - content_type(self) -> ContentTypeId
  - encode(self, content: Attachment, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> Attachment
  - fallback(self, content: Attachment) -> str | None
  - should_push(self, content: Attachment) -> bool
- class RemoteAttachmentCodec(ContentCodec[RemoteAttachment]): Codec for remote attachment metadata.
  - content_type(self) -> ContentTypeId
  - encode(self, content: RemoteAttachment, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> RemoteAttachment
  - fallback(self, content: RemoteAttachment) -> str | None
  - should_push(self, content: RemoteAttachment) -> bool

### Module: xmtp_content_type_reply

Doc: Reply content type for XMTP.

Exports: ContentTypeReply, Reply, ReplyCodec

Constants:
- ContentTypeReply = ContentTypeId(authority_id='xmtp.org', type_id='reply', version_major=1, version_minor=0)

Classes:
- class Reply: Reply content wrapper.
- class ReplyCodec(ContentCodec[Reply]): Codec for reply messages.
  - content_type(self) -> ContentTypeId
  - encode(self, content: Reply, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> Reply
  - fallback(self, content: Reply) -> str | None
  - should_push(self, content: Reply) -> bool

### Module: xmtp_content_type_text

Doc: Text content type for XMTP.

Exports: ContentTypeText, Encoding, TextCodec

Constants:
- ContentTypeText = ContentTypeId(authority_id='xmtp.org', type_id='text', version_major=1, version_minor=0)

Classes:
- class Encoding(str, Enum)
- class TextCodec(ContentCodec[str]): Codec for plain text messages.
  - content_type(self) -> ContentTypeId
  - encode(self, content: str, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> str
  - fallback(self, content: str) -> str | None
  - should_push(self, content: str) -> bool

### Module: xmtp_content_type_transaction_reference

Doc: Transaction reference content type for XMTP.

Exports: ContentTypeTransactionReference, TransactionMetadata, TransactionReference, TransactionReferenceCodec

Constants:
- ContentTypeTransactionReference = ContentTypeId(authority_id='xmtp.org', type_id='transactionReference', version_major=1, version_minor=0)

Classes:
- class TransactionMetadata: Optional transaction metadata.
- class TransactionReference: Transaction reference content.
- class TransactionReferenceCodec(ContentCodec[TransactionReference]): Codec for transaction references.
  - content_type(self) -> ContentTypeId
  - encode(self, content: TransactionReference, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> TransactionReference
  - fallback(self, content: TransactionReference) -> str | None
  - should_push(self, content: TransactionReference) -> bool

### Module: xmtp_content_type_wallet_send_calls

Doc: Wallet send calls content type for XMTP.

Exports: ContentTypeWalletSendCalls, WalletCall, WalletCallMetadata, WalletSendCalls, WalletSendCallsCodec

Constants:
- ContentTypeWalletSendCalls = ContentTypeId(authority_id='xmtp.org', type_id='walletSendCalls', version_major=1, version_minor=0)

Classes:
- class WalletCallMetadata: Metadata describing a wallet call.
- class WalletCall: Wallet call fields.
- class WalletSendCalls: Wallet send calls payload.
- class WalletSendCallsCodec(ContentCodec[WalletSendCalls]): Codec for wallet send calls.
  - content_type(self) -> ContentTypeId
  - encode(self, content: WalletSendCalls, registry: CodecRegistry | None = None) -> EncodedContent
  - decode(self, content: EncodedContent, registry: CodecRegistry | None = None) -> WalletSendCalls
  - fallback(self, content: WalletSendCalls) -> str | None
  - should_push(self, content: WalletSendCalls) -> bool

## Bindings API Reference

### Module: xmtp_bindings

Doc: Python bindings for libxmtp generated via UniFFI.

### Module: xmtp_bindings.build

Doc: Build helpers for libxmtp native library.

Classes:
- class BuildPy(build_py): Ensure libxmtp is built before packaging.
  - run(self) -> None
- class BuildExt(build_ext): Ensure libxmtp is built before extensions (even if none exist).
  - run(self) -> None
- class Develop(develop): Ensure libxmtp is built for editable installs.
  - run(self) -> None
- class Install(install): Install pure-Python packages into platlib for bundled shared libraries.
  - finalize_options(self) -> None
- class BdistWheel(bdist_wheel): Mark the wheel as platform-specific when bundling native libraries.
  - finalize_options(self) -> None
  - run(self) -> None
  - write_wheelfile(self, *args: object, **kwargs: object) -> None

### Module: xmtp_bindings.xmtpv3

Exports: InternalError, FfiActionStyle, FfiBackupElementSelection, FfiClientMode, FfiConsentEntityType, FfiConsentState, FfiContentType, FfiConversationMessageKind, FfiConversationType, FfiCryptoError, FfiDecodedMessageBody, FfiDecodedMessageContent, FfiDeliveryStatus, FfiDirection, FfiForkRecoveryPolicy, FfiGroupMembershipState, FfiGroupMessageKind, FfiGroupPermissionsOptions, FfiGroupQueryOrderBy, FfiIdentifierKind, FfiLogLevel, FfiLogRotation, FfiMetadataField, FfiPermissionLevel, FfiPermissionPolicy, FfiPermissionUpdateType, FfiPreferenceUpdate, FfiProcessType, FfiReactionAction, FfiReactionSchema, FfiSignatureKind, FfiSortBy, FfiSubscribeError, FfiSyncMetric, FfiSyncWorkerMode, GenericError, IdentityValidationError, SigningError, FfiAction, FfiActions, FfiApiStats, FfiArchiveOptions, FfiAttachment, FfiBackupMetadata, FfiConsent, FfiContentTypeId, FfiConversationDebugInfo, FfiConversationMember, FfiCreateDmOptions, FfiCreateGroupOptions, FfiCredential, FfiCursor, FfiDecodedMessageMetadata, FfiEncodedContent, FfiEnrichedReply, FfiForkRecoveryOpts, FfiGroupSyncSummary, FfiGroupUpdated, FfiHmacKey, FfiIdentifier, FfiIdentityStats, FfiInbox, FfiInboxState, FfiInstallation, FfiIntent, FfiKeyPackageStatus, FfiLeaveRequest, FfiLifetime, FfiListConversationsOptions, FfiListMessagesOptions, FfiMarkdownContent, FfiMessage, FfiMessageDisappearingSettings, FfiMessageMetadata, FfiMessageWithReactions, FfiMetadataFieldChange, FfiMultiRemoteAttachment, FfiPasskeySignature, FfiPermissionPolicySet, FfiReactionPayload, FfiReadReceipt, FfiRemoteAttachment, FfiRemoteAttachmentInfo, FfiReply, FfiSendMessageOpts, FfiTextContent, FfiTransactionMetadata, FfiTransactionReference, FfiUpdateGroupMembershipResult, FfiWalletCall, FfiWalletCallMetadata, FfiWalletSendCalls, apply_signature_request, connect_to_backend, create_client, decode_actions, decode_attachment, decode_group_updated, decode_intent, decode_leave_request, decode_markdown, decode_multi_remote_attachment, decode_reaction, decode_read_receipt, decode_remote_attachment, decode_reply, decode_text, decode_transaction_reference, decode_wallet_send_calls, encode_actions, encode_attachment, encode_intent, encode_leave_request, encode_markdown, encode_multi_remote_attachment, encode_reaction, encode_read_receipt, encode_remote_attachment, encode_reply, encode_text, encode_transaction_reference, encode_wallet_send_calls, enter_debug_writer, enter_debug_writer_with_level, ethereum_address_from_pubkey, ethereum_generate_public_key, ethereum_hash_personal, ethereum_sign_recoverable, exit_debug_writer, generate_inbox_id, get_inbox_id_for_identifier, get_newest_message_metadata, get_version_info, inbox_state_from_inbox_ids, is_connected, revoke_installations, FfiAuthCallback, FfiAuthHandle, FfiConsentCallback, FfiConversation, FfiConversationCallback, FfiConversationListItem, FfiConversationMetadata, FfiConversations, FfiDecodedMessage, FfiGroupPermissions, FfiInboxOwner, FfiMessageCallback, FfiMessageDeletionCallback, FfiPreferenceCallback, FfiSignatureRequest, FfiStreamCloser, FfiSyncWorker, FfiXmtpClient, XmtpApiClient

Constants:
- FfiCryptoError = _UniffiTempFfiCryptoError
- FfiSubscribeError = _UniffiTempFfiSubscribeError
- GenericError = _UniffiTempGenericError
- IdentityValidationError = _UniffiTempIdentityValidationError
- SigningError = _UniffiTempSigningError

Classes:
- class InternalError(Exception)
- class FfiAction
- class FfiActions
- class FfiApiStats
- class FfiArchiveOptions
- class FfiAttachment
- class FfiBackupMetadata
- class FfiConsent
- class FfiContentTypeId
- class FfiConversationDebugInfo
- class FfiConversationMember
- class FfiCreateDmOptions
- class FfiCreateGroupOptions
- class FfiCredential
- class FfiCursor
- class FfiDecodedMessageMetadata
- class FfiEncodedContent
- class FfiEnrichedReply
- class FfiForkRecoveryOpts
- class FfiGroupSyncSummary
- class FfiGroupUpdated
- class FfiHmacKey
- class FfiIdentifier
- class FfiIdentityStats
- class FfiInbox
- class FfiInboxState
- class FfiInstallation
- class FfiIntent
- class FfiKeyPackageStatus
- class FfiLeaveRequest: Represents a leave request message sent when a user wants to leave a group.
- class FfiLifetime
- class FfiListConversationsOptions
- class FfiListMessagesOptions
- class FfiMarkdownContent
- class FfiMessage
- class FfiMessageDisappearingSettings: Settings for disappearing messages in a conversation.
- class FfiMessageMetadata
- class FfiMessageWithReactions
- class FfiMetadataFieldChange
- class FfiMultiRemoteAttachment
- class FfiPasskeySignature
- class FfiPermissionPolicySet
- class FfiReactionPayload
- class FfiReadReceipt
- class FfiRemoteAttachment
- class FfiRemoteAttachmentInfo
- class FfiReply
- class FfiSendMessageOpts
- class FfiTextContent
- class FfiTransactionMetadata
- class FfiTransactionReference
- class FfiUpdateGroupMembershipResult
- class FfiWalletCall
- class FfiWalletCallMetadata
- class FfiWalletSendCalls
- class FfiActionStyle(enum.Enum)
- class FfiBackupElementSelection(enum.Enum)
- class FfiClientMode(enum.Enum)
- class FfiConsentEntityType(enum.Enum)
- class FfiConsentState(enum.Enum)
- class FfiContentType(enum.Enum)
- class FfiConversationMessageKind(enum.Enum)
- class FfiConversationType(enum.Enum)
- class FfiCryptoError(Exception)
- class FfiCryptoError
- class FfiDecodedMessageBody
  - is_TEXT(self) -> bool
  - is_text(self) -> bool
  - is_MARKDOWN(self) -> bool
  - is_markdown(self) -> bool
  - is_REACTION(self) -> bool
  - is_reaction(self) -> bool
  - is_ATTACHMENT(self) -> bool
  - is_attachment(self) -> bool
  - is_REMOTE_ATTACHMENT(self) -> bool
  - is_remote_attachment(self) -> bool
  - is_MULTI_REMOTE_ATTACHMENT(self) -> bool
  - is_multi_remote_attachment(self) -> bool
  - is_TRANSACTION_REFERENCE(self) -> bool
  - is_transaction_reference(self) -> bool
  - is_GROUP_UPDATED(self) -> bool
  - is_group_updated(self) -> bool
  - is_READ_RECEIPT(self) -> bool
  - is_read_receipt(self) -> bool
  - is_WALLET_SEND_CALLS(self) -> bool
  - is_wallet_send_calls(self) -> bool
  - is_INTENT(self) -> bool
  - is_intent(self) -> bool
  - is_ACTIONS(self) -> bool
  - is_actions(self) -> bool
  - is_LEAVE_REQUEST(self) -> bool
  - is_leave_request(self) -> bool
  - is_CUSTOM(self) -> bool
  - is_custom(self) -> bool
- class FfiDecodedMessageContent
  - is_TEXT(self) -> bool
  - is_text(self) -> bool
  - is_MARKDOWN(self) -> bool
  - is_markdown(self) -> bool
  - is_REPLY(self) -> bool
  - is_reply(self) -> bool
  - is_REACTION(self) -> bool
  - is_reaction(self) -> bool
  - is_ATTACHMENT(self) -> bool
  - is_attachment(self) -> bool
  - is_REMOTE_ATTACHMENT(self) -> bool
  - is_remote_attachment(self) -> bool
  - is_MULTI_REMOTE_ATTACHMENT(self) -> bool
  - is_multi_remote_attachment(self) -> bool
  - is_TRANSACTION_REFERENCE(self) -> bool
  - is_transaction_reference(self) -> bool
  - is_GROUP_UPDATED(self) -> bool
  - is_group_updated(self) -> bool
  - is_READ_RECEIPT(self) -> bool
  - is_read_receipt(self) -> bool
  - is_WALLET_SEND_CALLS(self) -> bool
  - is_wallet_send_calls(self) -> bool
  - is_INTENT(self) -> bool
  - is_intent(self) -> bool
  - is_ACTIONS(self) -> bool
  - is_actions(self) -> bool
  - is_LEAVE_REQUEST(self) -> bool
  - is_leave_request(self) -> bool
  - is_CUSTOM(self) -> bool
  - is_custom(self) -> bool
- class FfiDeliveryStatus(enum.Enum)
- class FfiDirection(enum.Enum)
- class FfiForkRecoveryPolicy(enum.Enum)
- class FfiGroupMembershipState(enum.Enum)
- class FfiGroupMessageKind(enum.Enum)
- class FfiGroupPermissionsOptions(enum.Enum)
- class FfiGroupQueryOrderBy(enum.Enum)
- class FfiIdentifierKind(enum.Enum)
- class FfiLogLevel(enum.Enum): Enum representing log levels
- class FfiLogRotation(enum.Enum): Enum representing log file rotation options
- class FfiMetadataField(enum.Enum)
- class FfiPermissionLevel(enum.Enum)
- class FfiPermissionPolicy(enum.Enum)
- class FfiPermissionUpdateType(enum.Enum)
- class FfiPreferenceUpdate
  - is_HMAC(self) -> bool
  - is_hmac(self) -> bool
- class FfiProcessType(enum.Enum): Enum representing process types for logging
- class FfiReactionAction(enum.Enum)
- class FfiReactionSchema(enum.Enum)
- class FfiSignatureKind(enum.Enum): Signature kind used in identity operations
- class FfiSortBy(enum.Enum)
- class FfiSubscribeError(Exception)
- class FfiSubscribeError
- class FfiSyncMetric(enum.Enum)
- class FfiSyncWorkerMode(enum.Enum)
- class GenericError(Exception)
- class GenericError
- class IdentityValidationError(Exception)
- class IdentityValidationError
- class SigningError(Exception)
- class SigningError
- class FfiAuthCallback
  - on_auth_required(self)
- class FfiConsentCallback
  - on_consent_update(self, consent: 'typing.List[FfiConsent]')
  - on_error(self, error: 'FfiSubscribeError')
  - on_close(self)
- class FfiConversationCallback
  - on_conversation(self, conversation: 'FfiConversation')
  - on_error(self, error: 'FfiSubscribeError')
  - on_close(self)
- class FfiInboxOwner
  - get_identifier(self)
  - sign(self, text: 'str')
- class FfiMessageCallback
  - on_message(self, message: 'FfiMessage')
  - on_error(self, error: 'FfiSubscribeError')
  - on_close(self)
- class FfiMessageDeletionCallback
  - on_message_deleted(self, message_id: 'bytes')
- class FfiPreferenceCallback
  - on_preference_update(self, preference: 'typing.List[FfiPreferenceUpdate]')
  - on_error(self, error: 'FfiSubscribeError')
  - on_close(self)
- class FfiAuthHandle
  - id(self) -> 'int'
  - async set(self, credential: 'FfiCredential') -> None
- class FfiConversation
  - async add_admin(self, inbox_id: 'str') -> None
  - async add_members(self, account_identifiers: 'typing.List[FfiIdentifier]') -> 'FfiUpdateGroupMembershipResult'
  - async add_members_by_inbox_id(self, inbox_ids: 'typing.List[str]') -> 'FfiUpdateGroupMembershipResult'
  - async add_super_admin(self, inbox_id: 'str') -> None
  - added_by_inbox_id(self) -> 'str'
  - admin_list(self) -> 'typing.List[str]'
  - app_data(self) -> 'str'
  - consent_state(self) -> 'FfiConsentState'
  - async conversation_debug_info(self) -> 'FfiConversationDebugInfo'
  - conversation_message_disappearing_settings(self) -> 'typing.Optional[FfiMessageDisappearingSettings]'
  - conversation_type(self) -> 'FfiConversationType'
  - count_messages(self, opts: 'FfiListMessagesOptions') -> 'int'
  - created_at_ns(self) -> 'int'
  - dm_peer_inbox_id(self) -> 'typing.Optional[str]'
  - async find_duplicate_dms(self) -> 'typing.List[FfiConversation]'
  - find_enriched_messages(self, opts: 'FfiListMessagesOptions') -> 'typing.List[FfiDecodedMessage]'
  - async find_messages(self, opts: 'FfiListMessagesOptions') -> 'typing.List[FfiMessage]'
  - find_messages_with_reactions(self, opts: 'FfiListMessagesOptions') -> 'typing.List[FfiMessageWithReactions]'
  - get_hmac_keys(self) -> 'dict[bytes, typing.List[FfiHmacKey]]'
  - get_last_read_times(self) -> 'dict[str, int]'
  - group_description(self) -> 'str'
  - group_image_url_square(self) -> 'str'
  - async group_metadata(self) -> 'FfiConversationMetadata'
  - group_name(self) -> 'str'
  - group_permissions(self) -> 'FfiGroupPermissions'
  - id(self) -> 'bytes'
  - is_active(self) -> 'bool'
  - is_admin(self, inbox_id: 'str') -> 'bool'
  - is_conversation_message_disappearing_enabled(self) -> 'bool'
  - is_super_admin(self, inbox_id: 'str') -> 'bool'
  - async leave_group(self) -> None
  - async list_members(self) -> 'typing.List[FfiConversationMember]'
  - membership_state(self) -> 'FfiGroupMembershipState'
  - paused_for_version(self) -> 'typing.Optional[str]'
  - async process_streamed_conversation_message(self, envelope_bytes: 'bytes') -> 'typing.List[FfiMessage]'
  - async publish_messages(self) -> None: Publish all unpublished messages
  - async remove_admin(self, inbox_id: 'str') -> None
  - async remove_conversation_message_disappearing_settings(self) -> None
  - async remove_members(self, account_identifiers: 'typing.List[FfiIdentifier]') -> None
  - async remove_members_by_inbox_id(self, inbox_ids: 'typing.List[str]') -> None
  - async remove_super_admin(self, inbox_id: 'str') -> None
  - async send(self, content_bytes: 'bytes', opts: 'FfiSendMessageOpts') -> 'bytes'
  - send_optimistic(self, content_bytes: 'bytes', opts: 'FfiSendMessageOpts') -> 'bytes': send a message without immediately publishing to the delivery service.
  - async send_text(self, text: 'str') -> 'bytes'
  - async stream(self, message_callback: 'FfiMessageCallback') -> 'FfiStreamCloser'
  - super_admin_list(self) -> 'typing.List[str]'
  - async sync(self) -> None
  - async update_app_data(self, app_data: 'str') -> None
  - update_consent_state(self, state: 'FfiConsentState') -> None
  - async update_conversation_message_disappearing_settings(self, settings: 'FfiMessageDisappearingSettings') -> None
  - async update_group_description(self, group_description: 'str') -> None
  - async update_group_image_url_square(self, group_image_url_square: 'str') -> None
  - async update_group_name(self, group_name: 'str') -> None
  - async update_permission_policy(self, permission_update_type: 'FfiPermissionUpdateType', permission_policy_option: 'FfiPermissionPolicy', metadata_field: 'typing.Optional[FfiMetadataField]') -> None
- class FfiConversationListItem
  - conversation(self) -> 'FfiConversation'
  - is_commit_log_forked(self) -> 'typing.Optional[bool]'
  - last_message(self) -> 'typing.Optional[FfiMessage]'
- class FfiConversationMetadata
  - conversation_type(self) -> 'FfiConversationType'
  - creator_inbox_id(self) -> 'str'
- class FfiConversations
  - async create_group(self, account_identities: 'typing.List[FfiIdentifier]', opts: 'FfiCreateGroupOptions') -> 'FfiConversation'
  - create_group_optimistic(self, opts: 'FfiCreateGroupOptions') -> 'FfiConversation'
  - async create_group_with_inbox_ids(self, inbox_ids: 'typing.List[str]', opts: 'FfiCreateGroupOptions') -> 'FfiConversation'
  - async find_or_create_dm(self, target_identity: 'FfiIdentifier', opts: 'FfiCreateDmOptions') -> 'FfiConversation'
  - async find_or_create_dm_by_inbox_id(self, inbox_id: 'str', opts: 'FfiCreateDmOptions') -> 'FfiConversation'
  - get_hmac_keys(self) -> 'dict[bytes, typing.List[FfiHmacKey]]'
  - list(self, opts: 'FfiListConversationsOptions') -> 'typing.List[FfiConversationListItem]'
  - list_dms(self, opts: 'FfiListConversationsOptions') -> 'typing.List[FfiConversationListItem]'
  - list_groups(self, opts: 'FfiListConversationsOptions') -> 'typing.List[FfiConversationListItem]'
  - async process_streamed_welcome_message(self, envelope_bytes: 'bytes') -> 'typing.List[FfiConversation]'
  - async stream(self, callback: 'FfiConversationCallback') -> 'FfiStreamCloser'
  - async stream_all_dm_messages(self, message_callback: 'FfiMessageCallback', consent_states: 'typing.Optional[typing.List[FfiConsentState]]') -> 'FfiStreamCloser'
  - async stream_all_group_messages(self, message_callback: 'FfiMessageCallback', consent_states: 'typing.Optional[typing.List[FfiConsentState]]') -> 'FfiStreamCloser'
  - async stream_all_messages(self, message_callback: 'FfiMessageCallback', consent_states: 'typing.Optional[typing.List[FfiConsentState]]') -> 'FfiStreamCloser'
  - async stream_consent(self, callback: 'FfiConsentCallback') -> 'FfiStreamCloser': Get notified when there is a new consent update either locally or is synced from another device
  - async stream_dms(self, callback: 'FfiConversationCallback') -> 'FfiStreamCloser'
  - async stream_groups(self, callback: 'FfiConversationCallback') -> 'FfiStreamCloser'
  - async stream_message_deletions(self, callback: 'FfiMessageDeletionCallback') -> 'FfiStreamCloser': Get notified when a message is deleted by the disappearing messages worker.
  - async stream_messages(self, message_callback: 'FfiMessageCallback', conversation_type: 'typing.Optional[FfiConversationType]', consent_states: 'typing.Optional[typing.List[FfiConsentState]]') -> 'FfiStreamCloser'
  - async stream_preferences(self, callback: 'FfiPreferenceCallback') -> 'FfiStreamCloser': Get notified when a preference changes either locally or is synced from another device
  - async sync(self) -> None
  - async sync_all_conversations(self, consent_states: 'typing.Optional[typing.List[FfiConsentState]]') -> 'FfiGroupSyncSummary'
- class FfiDecodedMessage
  - content(self) -> 'FfiDecodedMessageContent'
  - content_type_id(self) -> 'FfiContentTypeId'
  - conversation_id(self) -> 'bytes'
  - delivery_status(self) -> 'FfiDeliveryStatus'
  - expires_at_ns(self) -> 'typing.Optional[int]'
  - fallback_text(self) -> 'typing.Optional[str]'
  - has_reactions(self) -> 'bool'
  - id(self) -> 'bytes'
  - inserted_at_ns(self) -> 'int'
  - kind(self) -> 'FfiGroupMessageKind'
  - num_replies(self) -> 'int'
  - reaction_count(self) -> 'int'
  - reactions(self) -> 'typing.List[FfiDecodedMessage]'
  - sender_inbox_id(self) -> 'str'
  - sender_installation_id(self) -> 'bytes'
  - sent_at_ns(self) -> 'int'
- class FfiGroupPermissions
  - policy_set(self) -> 'FfiPermissionPolicySet'
  - policy_type(self) -> 'FfiGroupPermissionsOptions'
- class FfiSignatureRequest
  - async add_ecdsa_signature(self, signature_bytes: 'bytes') -> None
  - async add_passkey_signature(self, signature: 'FfiPasskeySignature') -> None
  - async add_scw_signature(self, signature_bytes: 'bytes', address: 'str', chain_id: 'int', block_number: 'typing.Optional[int]') -> None
  - async is_ready(self) -> 'bool'
  - async missing_address_signatures(self) -> 'typing.List[str]': missing signatures that are from `MemberKind::Address`
  - async signature_text(self) -> 'str'
- class FfiStreamCloser
  - end(self) -> None: Signal the stream to end
  - async end_and_wait(self) -> None: End the stream and asynchronously wait for it to shutdown
  - is_closed(self) -> 'bool'
  - async wait_for_ready(self) -> None
- class FfiSyncWorker
  - async wait(self, metric: 'FfiSyncMetric', count: 'int') -> None
- class FfiXmtpClient
  - async add_identity(self, new_identity: 'FfiIdentifier') -> 'FfiSignatureRequest': Adds a wallet address to the existing client
  - async addresses_from_inbox_id(self, refresh_from_network: 'bool', inbox_ids: 'typing.List[str]') -> 'typing.List[FfiInboxState]': * Get the inbox state for each `inbox_id`.
  - api_aggregate_statistics(self) -> 'str'
  - api_identity_statistics(self) -> 'FfiIdentityStats'
  - api_statistics(self) -> 'FfiApiStats'
  - async apply_signature_request(self, signature_request: 'FfiSignatureRequest') -> None
  - async archive_metadata(self, path: 'str', key: 'bytes') -> 'FfiBackupMetadata': Load the metadata for an archive to see what it contains.
  - async can_message(self, account_identifiers: 'typing.List[FfiIdentifier]') -> 'dict[FfiIdentifier, bool]'
  - async change_recovery_identifier(self, new_recovery_identifier: 'FfiIdentifier') -> 'FfiSignatureRequest': * Change the recovery identifier for your inboxId
  - clear_all_statistics(self) -> None
  - conversation(self, conversation_id: 'bytes') -> 'FfiConversation'
  - conversations(self) -> 'FfiConversations'
  - async create_archive(self, path: 'str', opts: 'FfiArchiveOptions', key: 'bytes') -> None: Archive application elements to file for later restoration.
  - async db_reconnect(self) -> None
  - delete_message(self, message_id: 'bytes') -> 'int'
  - dm_conversation(self, target_inbox_id: 'str') -> 'FfiConversation'
  - enriched_message(self, message_id: 'bytes') -> 'FfiDecodedMessage'
  - async find_inbox_id(self, identifier: 'FfiIdentifier') -> 'typing.Optional[str]'
  - async get_consent_state(self, entity_type: 'FfiConsentEntityType', entity: 'str') -> 'FfiConsentState'
  - async get_key_package_statuses_for_installation_ids(self, installation_ids: 'typing.List[bytes]') -> 'dict[bytes, FfiKeyPackageStatus]'
  - async get_latest_inbox_state(self, inbox_id: 'str') -> 'FfiInboxState'
  - async import_archive(self, path: 'str', key: 'bytes') -> None: Import a previous archive
  - inbox_id(self) -> 'str'
  - async inbox_state(self, refresh_from_network: 'bool') -> 'FfiInboxState': * Get the client's inbox state.
  - installation_id(self) -> 'bytes'
  - message(self, message_id: 'bytes') -> 'FfiMessage'
  - async register_identity(self, signature_request: 'FfiSignatureRequest') -> None
  - release_db_connection(self) -> None
  - async revoke_all_other_installations_signature_request(self) -> 'typing.Optional[FfiSignatureRequest]': * Revokes all installations except the one the client is currently using
  - async revoke_identity(self, identifier: 'FfiIdentifier') -> 'FfiSignatureRequest': Revokes or removes an identity from the existing client
  - async revoke_installations(self, installation_ids: 'typing.List[bytes]') -> 'FfiSignatureRequest': * Revoke a list of installations
  - async send_sync_request(self) -> None: Manually trigger a device sync request to sync records from another active device on this account.
  - async set_consent_states(self, records: 'typing.List[FfiConsent]') -> None
  - sign_with_installation_key(self, text: 'str') -> 'bytes': A utility function to sign a piece of text with this installation's private key.
  - signature_request(self) -> 'typing.Optional[FfiSignatureRequest]'
  - async sync_preferences(self) -> 'FfiGroupSyncSummary'
  - verify_signed_with_installation_key(self, signature_text: 'str', signature_bytes: 'bytes') -> None: A utility function to easily verify that a piece of text was signed by this installation.
  - verify_signed_with_public_key(self, signature_text: 'str', signature_bytes: 'bytes', public_key: 'bytes') -> None: A utility function to easily verify that a string has been signed by another libXmtp installation.
- class XmtpApiClient: the opaque Xmtp Api Client for iOS/Android bindings

Functions:
- async apply_signature_request(api: 'XmtpApiClient', signature_request: 'FfiSignatureRequest') -> None: * Static apply a signature request
- async connect_to_backend(v3_host: 'str', gateway_host: 'typing.Optional[str]', is_secure: 'bool', client_mode: 'typing.Optional[FfiClientMode]', app_version: 'typing.Optional[str]', auth_callback: 'typing.Optional[FfiAuthCallback]', auth_handle: 'typing.Optional[FfiAuthHandle]') -> 'XmtpApiClient': connect to the XMTP backend
- async create_client(api: 'XmtpApiClient', sync_api: 'XmtpApiClient', db: 'typing.Optional[str]', encryption_key: 'typing.Optional[bytes]', inbox_id: 'str', account_identifier: 'FfiIdentifier', nonce: 'int', legacy_signed_private_key_proto: 'typing.Optional[bytes]', device_sync_server_url: 'typing.Optional[str]', device_sync_mode: 'typing.Optional[FfiSyncWorkerMode]', allow_offline: 'typing.Optional[bool]', fork_recovery_opts: 'typing.Optional[FfiForkRecoveryOpts]') -> 'FfiXmtpClient': It returns a new client of the specified `inbox_id`.
- decode_actions(bytes: 'bytes') -> 'FfiActions'
- decode_attachment(bytes: 'bytes') -> 'FfiAttachment'
- decode_group_updated(bytes: 'bytes') -> 'FfiGroupUpdated'
- decode_intent(bytes: 'bytes') -> 'FfiIntent'
- decode_leave_request(bytes: 'bytes') -> 'FfiLeaveRequest'
- decode_markdown(bytes: 'bytes') -> 'str'
- decode_multi_remote_attachment(bytes: 'bytes') -> 'FfiMultiRemoteAttachment'
- decode_reaction(bytes: 'bytes') -> 'FfiReactionPayload'
- decode_read_receipt(bytes: 'bytes') -> 'FfiReadReceipt'
- decode_remote_attachment(bytes: 'bytes') -> 'FfiRemoteAttachment'
- decode_reply(bytes: 'bytes') -> 'FfiReply'
- decode_text(bytes: 'bytes') -> 'str'
- decode_transaction_reference(bytes: 'bytes') -> 'FfiTransactionReference'
- decode_wallet_send_calls(bytes: 'bytes') -> 'FfiWalletSendCalls'
- encode_actions(actions: 'FfiActions') -> 'bytes'
- encode_attachment(attachment: 'FfiAttachment') -> 'bytes'
- encode_intent(intent: 'FfiIntent') -> 'bytes'
- encode_leave_request(request: 'FfiLeaveRequest') -> 'bytes'
- encode_markdown(text: 'str') -> 'bytes'
- encode_multi_remote_attachment(ffi_multi_remote_attachment: 'FfiMultiRemoteAttachment') -> 'bytes'
- encode_reaction(reaction: 'FfiReactionPayload') -> 'bytes'
- encode_read_receipt(read_receipt: 'FfiReadReceipt') -> 'bytes'
- encode_remote_attachment(remote_attachment: 'FfiRemoteAttachment') -> 'bytes'
- encode_reply(reply: 'FfiReply') -> 'bytes'
- encode_text(text: 'str') -> 'bytes'
- encode_transaction_reference(reference: 'FfiTransactionReference') -> 'bytes'
- encode_wallet_send_calls(wallet_send_calls: 'FfiWalletSendCalls') -> 'bytes'
- enter_debug_writer(directory: 'str', log_level: 'FfiLogLevel', rotation: 'FfiLogRotation', max_files: 'int', process_type: 'FfiProcessType') -> None: turns on logging to a file on-disk in the directory specified.
- enter_debug_writer_with_level(directory: 'str', rotation: 'FfiLogRotation', max_files: 'int', log_level: 'FfiLogLevel', process_type: 'FfiProcessType') -> None: turns on logging to a file on-disk with a specified log level.
- ethereum_address_from_pubkey(pubkey: 'bytes') -> 'str': 3) Ethereum address from public key (accepts 65-byte 0x04||XY or 64-byte XY).
- ethereum_generate_public_key(private_key32: 'bytes') -> 'bytes': 1) Ethereum compatible public key from 32-byte private key.
- ethereum_hash_personal(message: 'str') -> 'bytes': 4) EIP-191 personal message hash: keccak256("Ethereum Signed Message:
- ethereum_sign_recoverable(msg: 'bytes', private_key32: 'bytes', hashing: 'bool') -> 'bytes': 2) Ethereum recoverable signature (FFI).
- exit_debug_writer() -> None: Flush loglines from libxmtp log writer to the file, ensuring logs are written.
- generate_inbox_id(account_identifier: 'FfiIdentifier', nonce: 'int') -> 'str'
- async get_inbox_id_for_identifier(api: 'XmtpApiClient', account_identifier: 'FfiIdentifier') -> 'typing.Optional[str]'
- async get_newest_message_metadata(api: 'XmtpApiClient', group_ids: 'typing.List[bytes]') -> 'dict[bytes, FfiMessageMetadata]'
- get_version_info() -> 'str'
- async inbox_state_from_inbox_ids(api: 'XmtpApiClient', inbox_ids: 'typing.List[str]') -> 'typing.List[FfiInboxState]': * Static Get the inbox state for each `inbox_id`.
- async is_connected(api: 'XmtpApiClient') -> 'bool'
- revoke_installations(api: 'XmtpApiClient', recovery_identifier: 'FfiIdentifier', inbox_id: 'str', installation_ids: 'typing.List[bytes]') -> 'FfiSignatureRequest': * Static revoke a list of installations
